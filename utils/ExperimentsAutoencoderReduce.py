from ModelAutoencoderSpectralReduce import *


def experiments_autoencoder_reduce(parameters, mean, standard_deviation):
    autoencoder_model_reduce = model_autoencoder_reduce(parameters, mean, standard_deviation)
    return autoencoder_model_reduce
